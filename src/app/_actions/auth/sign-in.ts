import { storeApi } from "@/services/api"
import { LoginAuthDto } from "@/types/api/generated"

export const signInAction = async ({ email, password }: LoginAuthDto) => {
  // try {
  //   const response = await storeApi.
  // } catch (error) {}
}
