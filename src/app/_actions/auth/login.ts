import { LoginRequest, LoginResponse } from "@/types/api/generated"

export const signInAction = async ({
  email,
  password
}: LoginRequest): Promise<LoginResponse> => {
  try {
    const response = {} as LoginResponse
  } catch (error) {}
}
